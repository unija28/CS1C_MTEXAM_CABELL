let studentName = prompt("Enter student name:");
let subjectTitle = prompt("Enter subject title:");
let classSchedule = prompt("Enter class schedule (Time, Days):");
let classroom = prompt("Enter classroom:");
let classInstuctor = prompt("Enter class instructor:");

console.log(`${studentName} is currently enrolled in ${subjectTitle} with a class schedule of ${classSchedule} at room ${classroom}.
The instructor for the subject is ${classInstratuctor}`);
